# Chess in python 

pos[0] - first symbol in the env (so we get the num of row)
ord[0] - convert str into number, a = 97, so ord[1] (if 1 is e) - ord('a') = 4(so we get the num of column) 

























